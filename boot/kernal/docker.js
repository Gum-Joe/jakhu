// Starts the required docker stuff
function boot() {
  // body...
  // Load mongodb
  if (process.env.WEB_DOCKER_I === undefined) {
    throw new error('h')
  }
}
module.exports = {boot: boot};
boot()
