language: node_js

node_js:
  - "0.12"
  - "0.11"
  - "0.10"
  - "stable" 
  - "iojs"

addons:
  apt:
    packages:
    - xbuild

env:
  - NODE_ENV=development

script: "make tests && cd app && xbuild /p:Configuration=Release *.sln"
  
  
