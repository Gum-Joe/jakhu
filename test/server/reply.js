// Tests the server replys
/**
var assert = require('assert');
var request = require('supertest');
var http = require('http');
var app = require("../../app.js");
var fs = require('fs');

describe('GET reply tests', function () {
  before(function (done) {
    app.start("basic");
    done();
  });
});*/
