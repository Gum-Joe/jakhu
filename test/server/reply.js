// Tests the server replys
var assert = require('assert');
var request = require('supertest');
var http = require('http');
var app = require("../../app.js");
var fs = require('fs');

describe('GET reply test', function () {
  it('should test the reply of GET /', function (done) {
    done();
  })
});
