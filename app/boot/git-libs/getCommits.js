var exec = require('child_process').exec;

/**exports.getCommits = function (x) {
  exec("git rev-list HEAD --count", function (error, stdout, stderr) {
      return stdout;
      return stderr;
  });
}*/